import React from 'react';
import { View } from 'react-native';

// Fallback component for TypeScript module resolution and unsupported platforms
export const AdBanner = () => {
  return <View />;
};
